
-- =====================================================
-- CHECAR STORAGE BUCKETS
-- Execute no SQL Editor (pode retornar vazio se não houver buckets)
-- =====================================================

SELECT id, name, public, created_at
FROM storage.buckets;
